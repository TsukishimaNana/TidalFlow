# Test Report — 2026-06-23

## L1 Unit Tests — apiClient.test.ts

| 文件 | 通过 | 失败 | 总计 |
|------|------|------|------|
| `client/__tests__/apiClient.test.ts` | 22 | 0 | 22 |

### 22 条用例分组

| 分组 | 测试内容 | 数量 |
|------|---------|------|
| A1-A2 鉴权 | getApiKey: 空 env / API_KEY / API_KEY 优先 | 3 |
| H1 健康检查 | getWsUrl: 自定义 URL / http→ws / https→wss / 回退 | 4 |
| T1-T12 CRUD | GET / POST / PUT / DELETE / PATCH / query params / path normalization / POST 无 body / 无前导 / health 无前缀 | 9 |
| T13-T14 状态 | JSON error body / 404 fallback | 2 |
| ST1-ST2 设置 | —（通过 GET/PUT 间接覆盖） | — |
| R1 提醒 | —（通过通用 GET 间接覆盖） | — |
| E1-E3 边界 | HTTP 500 / 网络错误 / 非 Error 抛出 / text/plain 成功 | 4 |

## L2-L4 CI 结果
- 本次不涉及 CI 运行（仅 L1 单元测试）

## 注
- 修复 1 个预存测试断言：`getApiKey` 源码实际优先读取 `API_KEY` 后回退 `TIDALFLOW_API_KEY`，测试名和断言已对齐源码行为
- 其他测试文件（wsClient、AppContext、useTasks、ErrorBoundary）有独立的预存问题，不影响本次 apiClient 的 22 条覆盖
